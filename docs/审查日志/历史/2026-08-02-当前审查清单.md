# 2026-08-02 当前审查清单

> ⚠️ **已废弃**:本清单内容已并入[2026-08-02 审查核验汇总](2026-08-02-审查核验汇总.md),仅作对照保留,不再维护;问题状态以核验汇总为准。

> 日期:2026-08-02。
> 类型:汇总清单。
> 性质:历史快照(已废弃,仅作对照)。
> 范围:`agent/`、`qqbot/`、`webui_py/`、Compose、Dockerfile 与环境变量模板。
> 说明:问题背景、方案比较与重构建议见[2026-08-01 优化报告](2026-08-01-优化报告.md)。

## P0 正确性与权限

| 问题 | 当前证据 | 建议动作 |
|------|----------|----------|
| `/` 命令无鉴权 | `task_worker.py::user_interface` 在校验 `QQ_TARGET_USER_ID` 前处理命令 | 命令分支复用授权校验,避免余额泄露和搜索配额滥用 |
| 出队后失败不重试 | LLM、工具或 QQ 发送异常后事件不会重新入队 | 先引入事件 ID,再实现有限重试与失败事件记录 |

## P1 健壮性与安全

| 问题 | 当前证据 | 建议动作 |
|------|----------|----------|
| 外部 HTTP 异常处理不统一 | 余额接口直接索引响应字段,QQ API 失败直接抛出 | 统一状态码、响应结构、超时和有限重试策略 |
| Worker 处理状态缺心跳 | `processing` 只有 `started_at`,没有 `updated_at` | 进入处理及长任务边界刷新 `updated_at` |
| Redis 客户端反复创建 | 两个 `queue_client.py` 每次调用都创建客户端 | 各进程复用模块级客户端 |
| 未使用 Key 阻塞启动 | `tool.py` 导入时强读 `ZAI_API_KEY`、`BOCHA_*` | 未启用的工具延迟读取配置 |
| 数据库与队列权限过大 | Redis 无密码,MongoDB 使用 root 应用连接 | 接入 `REDIS_PASSWORD`,创建最小权限 MongoDB 用户 |

## P2 精简与维护

| 问题 | 建议动作 |
|------|----------|
| `qqapi.py` 有重复请求模板和未使用函数 | 抽 `_post`,删除确认未使用的四个接口 |
| qqbot 的 `pop_from_queue` 未使用且方向错误 | 删除该函数;如需消费则统一使用 `BRPOP` |
| 工具 schema 与执行器分开维护 | 在测试覆盖后引入工具注册表 |
| 事件与命令使用长 `if/elif` | 在测试覆盖后改为分发表和命令路由表 |

## P3 WebUI

| 问题 | 建议动作 |
|------|----------|
| 无 favicon | 增加 data URI 或静态图标 |
| 搜索无 debounce | 对输入增加约 150ms 防抖 |
| done 行 ID 极低概率冲突 | 暴露 MongoDB `_id`,用它作为历史事件稳定 ID |

## 已确认的当前事实

- 生产 `docker-compose.yml` 会直接启动 agent 与 qqbot;`sleep infinity` 仅存在于开发覆盖配置。
- WebUI 已由 Go 迁移为 FastAPI(`webui_py/`,端口 8081),事件流页与终端页均已回归。
- MongoDB 已有鉴权健康检查,agent 与 webui_py 等待 `service_healthy`。
- `.env.example` 已包含 `REDIS_SOCKET_TIMEOUT` 与 `MONGO_DATABASE`。
- 工具消息采用文本投影,不使用原生 `tool` 角色,详见[格式规范](../../06-格式规范.md)。
- WebUI 已实现三区段顺序、稳定 pending ID、running 指纹去重与 DOM 行复用。

建议处理顺序:P0 → P1 → P2 → P3。每次只处理一个可验证的问题组,并同步删除本清单中的对应行。
